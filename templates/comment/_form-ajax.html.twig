{% form_theme form 'bootstrap_4_layout.html.twig' %}

{{ form_start(form) }}
    {{ form_widget(form) }}

    <div class="text-right mb-2">
        <button class="btn btn-success btn-sm">{{ 'app.save'|trans }}</button>
    </div>

{{ form_end(form) }}
